
export default function Integrantes() {

    // const {familia} = useAppSelector(state => state.config.familia)

    return (
        <>
            <h3>Integrantes</h3>
            <form>
                Integrantes
            </form>
        </>
    )
}